# job_hunter
A end to end pipeline that scrapes , analyzes and scores the scraped jobs based on the query entered by the user. Different Endpoints one with the feature of background jobs that allow user to run multiple queries.
